### <protocol name="{{protocol.name|safe}}" date="{{protocol.date|safe}}">
{% for speech in speeches %}
### <speech speaker="{{speech.speaker|safe}}", speech_index="{{loop.index}}" speech_date="{{protocol.date}}" speech_index="{{loop.index}}">
{{speech.text}}
{% endfor %}
### </speech>